export const fetchCityData = async (cityName) => {

    /*
    const response = await fetch(`https://api.example.com/cities/${cityName}`);
    const data = await response.json();
    */

    const data = "cityName: " + cityName;

    return data;
};